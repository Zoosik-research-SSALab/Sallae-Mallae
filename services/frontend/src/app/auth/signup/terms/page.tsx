import TermsSignupCard from "./components/TermsSignupCard";

export default function SocialTermsSignupPage() {
  return (
    <main className="flex min-h-[calc(100vh-72px)] items-center justify-center bg-[color:var(--color-bg-secondary)] px-4 py-10">
      <TermsSignupCard />
    </main>
  );
}
